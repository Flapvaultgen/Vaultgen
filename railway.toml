# Railway: Docker build (Node 22 + Foundry). Set ANTHROPIC_API_KEY in Railway Variables.
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
